# paraphrase corpus의 역할과 이력.
#
# 결과를 보고 결정을 내리는 데 한 번이라도 쓴 corpus는 더 이상 holdout이 아니다.
# 이름에 holdout이 들어 있어도 여기서 development로 표시한다. corpus 파일 자체는
# 고치지 않는다. 고치면 그 corpus로 잰 run의 meta에 남은 sha256과 어긋난다.
#
# role
#   development    후보를 고르거나 결정을 내리는 데 이미 쓴 것
#   fresh_holdout  아직 어떤 결과도 보지 않은 것. 한 번 쓰면 development로 바꾼다.
#
# sha256은 파일 본문이다. 테스트가 현재 파일과 대조해, 고정한 corpus가 바뀌면
# 걸린다.

corpora:
  - path: evaluation/paraphrases.yaml
    parents: [stub_query_boundary.yaml, stub_query.yaml]
    role: development
    sha256: a1a047c75914e415e8da67a966f970782a4e1f4576481db2a66308c385bb2f59
    history:
      - "68c5fe5 선택용 corpus로 작성 (12 intent, 68 paraphrase)"
      - "645bb28 C/D_PRE/T0 재검증에 사용"
      - "57ddf87 taxi_type 문구 변형 선택에 사용"
      - "20260924_005520_census_head 에서 current HEAD 실패 집계(census)에 사용. 결정에 쓰지 않음"
      - "20260925_011212_census_h2_production 에서 production H2 통합 census에 사용. 결정에 쓰지 않음"

  - path: evaluation/paraphrases_holdout.yaml
    parents: [evaluation/holdout_parents.yaml]
    role: development
    sha256: 3d5cd763be49b6d119cc890ad631f96fbd7afe1bb4f8aaaaf74947003b0fc275
    history:
      - "5e8eaa0 taxi_type 문구 검증용 holdout으로 작성 (15 intent, 60 paraphrase)"
      - "d9f4782 holdout 결과로 0ccabc3 rollback을 결정. 이후 development"
      - "20260924_005520_census_head 에서 current HEAD 실패 집계(census)에 사용. 결정에 쓰지 않음"
      - "20260925_011212_census_h2_production 에서 production H2 통합 census에 사용. 결정에 쓰지 않음"

  - path: evaluation/paraphrases_factor_holdout.yaml
    parents: [evaluation/factor_holdout_parents.yaml]
    role: development
    purpose: "50fae72 factor 안내 factorial(F00/F10/F01/F11)에서 고른 후보의 검증"
    sha256: 6c2d37e7bdde197ee43d8dcb3b0b17b35554fe73154e50d33a01842d60729e40
    history:
      - "1e90908 factorial development 측정 전에 fresh holdout으로 작성 (15 intent, 58 paraphrase)"
      - "20260923_204536_factor_holdout_f00_f11 에서 F00 vs F11 검증에 사용. 결정 E. 이후 development"
      - "20260924_005520_census_head 에서 current HEAD 실패 집계(census)에 사용. 결정에 쓰지 않음"
      - "20260925_011212_census_h2_production 에서 production H2 통합 census에 사용. 결정에 쓰지 않음"

  - path: evaluation/paraphrases_aggregation_holdout.yaml
    parents: [evaluation/aggregation_holdout_parents.yaml]
    role: development
    purpose: "두 단계 집계 grounding H0(flat factor) vs H2(aggregation_plan) 검증"
    sha256: c35b6d23f443c2f6115407ed9b59ea5e91e8987aaf50a680a97684ecedb867a4
    history:
      - "H2 arm 구현과 어떤 측정보다 먼저 작성 (23 intent, 69 paraphrase)"
      - "20260924_023250_aggregation_holdout_h0_h2 는 무효 관측으로 18건에서 멈춤. 결과를 보지 않음"
      - "20260924_032103_aggregation_holdout_h0_h2_r2 에서 H0 vs H2 판정에 사용. Case A. 이후 development"
      - "20260925_015316_aggregation_dev_h2_production 에서 production H2 동작 확인(한 arm). 판정에 쓰지 않음"
      - "20260925_024141_local_aggregation_dev_smoke 에서 L1 parser·patch·scope 확인(한 arm). 정확도는 보지 않음, 판정에 쓰지 않음"

  - path: evaluation/paraphrases_local_aggregation_holdout.yaml
    parents: [evaluation/local_aggregation_holdout_parents.yaml]
    role: development
    purpose: "국소 집계 보정 H0 vs L1(집계 전용 두 번째 호출) 검증"
    sha256: 5edd2247e1789853eaed84ef34e54fc19daa2fb70a76b16ff75dc9d71bdef409
    history:
      - "L1 보정 prompt(22c501ff)를 고정한 cb2d0e0 뒤, L1 측정 결과를 보기 전에 작성 (24 intent, 72 paraphrase)"
      - "20260925_121002_local_aggregation_holdout_h0_l1 에서 H0 vs L1 판정에 사용. Case B. 이후 development"
